# gitignore is a function that lists what it will always ignore and never put it on the internet

.Rproj.user
.Rhistory
.RData
.Ruserdata

# this is the default of what is ignored

# create a new textfile called "README.md"  from file--> new--> text file
# go to the G+ sign sideways, and go to dropdown menu and commit
